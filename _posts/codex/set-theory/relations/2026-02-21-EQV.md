---
layout: carbon
title: Equivalence Relation
categories: codex
section: set-theory relations
updated: 2026-02-22
ordinal: 40
---

> <span class="math-h">DEF-EQV. Equivalence Relation.</span>
>
> An **equivalence relation** is a binary relation that is reflexive, symmetric and transitive ([DEF-RP](/codex/RP#DEF-RP)).

> <span class="math-h">DEF-EQV-CLS. Equivalence Class.</span>
>
> Let $\sim$ be an equivalence relation on $X$. The **equivalence class** of an element $a$ from $X$ is
>
> $$ [a] = \{x\in X:a\sim x\}. $$

> <span class="math-h">PROP-EQV-CLS.</span>
>
> Let $\sim$ be an equivalence relation on $X$ and let $x$, $y\in X$. The following statements are equivalent:
>
> - $x\sim y$.
> - $[x]=[y]$.
> - $x\in[y]$.
> - $[x]\cap[y]\neq\varnothing$.

> <span class="math-h">DEF-EQV-QUO. Quotient Set.</span>
>
> Let $\sim$ be an equivalence relation on $X$. The **quotient set** of $X$ by $\sim$, or $X$ **modulo** $\sim$, is the set
>
> $$ X/{\sim} = \{[x]:x\in X\}. $$

