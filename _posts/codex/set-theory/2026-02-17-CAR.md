---
layout: carbon
title: Cartesian Product
categories: codex
area: set-theory
updated: 2026-02-21
ordinal: 130
---

> <span class="math-h">DEF-CAR. Cartesian Product ($\mathsf{ZF}$).</span>
>
> - The **Cartesian product** of two sets $X$ and $Y$, written as $X\times Y$, is the set
>
>   $$ X\times Y = \{(x,y)\in\mathcal{P}(\mathcal{P}(X\cup Y)):x\in X\land y\in Y\}. $$
>
> - Notice that, because in general $((x,y),z)\neq (x,(y,z))$ for arbitrary sets $x$, $y$ and $z$, $\times$ is technically not associative. Nevertheless, for threes sets $X$, $Y$ and $Z$, we define
>
>   $$ X\times Y\times Z = (X\times Y)\times Z. $$
>
> - For four sets $X$, $Y$, $Z$ and $U$, we define
>
>   $$ X\times Y\times Z\times U = (X\times Y\times Z)\times U. $$
>
> - Products of more sets are defined analogously.
>
> - If $n\in\mathbb{N}^+$, we write
>
>   $$ X^n = \underbrace{X\times\cdots\times X}_{\text{$n$ times}}. $$

> <span class="math-h">PROP-CAR-EMP.</span>
>
> For any set $X$,
>
> $$ X\times\varnothing = \varnothing\times X = \varnothing. $$

> <span class="math-h">PROP-CAR-ITSC.</span>
>
> For any four sets $A$, $B$, $C$ and $D$,
>
> $$ (A\cap B)\times(C\cap D) = (A\times C)\cap(B\times D). $$

> <span class="math-h">PROP-CAR-DIST.</span>
>
> For any three sets $A$, $B$ and $C$:
>
> - $A\times(B\cap C) = (A\times B)\cap(A\times C)$.
> - $A\times(B\cup C) = (A\times B)\cup(A\times C)$.
> - $A\times(B-C) = (A\times B)-(A\times C)$.

