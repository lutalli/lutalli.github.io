---
layout: carbon
title: Ring Homomorphism
updated: 2026-03-19
---

> {{ "RH#DEF:Ring Homomorphism" | cod }}
>
> Let $R$ and $S$ be two rings. A **ring homomorphism** between $R$ and $S$ is a function $f:R\to S$ such that:
>
> {:.par-upper-roman}
> 1. For all $a$, $b\in R$,
>
>    $$ f(a+b) = f(a)+f(b). $$
>
> 2. For all $a$, $b\in R$,
>
>    $$ f(ab) = f(a)f(b). $$
>
> 3. $$ f(1) = 1. $$
>
> In other words, $f$ is both a group homomorphism from $(R,+)$ to $(S,+)$ and a monoid homomorphism from $(R,\cdot)$ to $(S,\cdot)$.

> {{ "RH#DEF-KER:Kernel" | cod }}
>
> The **kernel** of a ring homomorphism $f:R\to S$ is
>
> $$ \ker f = f^{-1}[\{0\}]. $$

---

> {{ "RH#PROP-A" | cod }}
>
> Let $f:R\to S$ is a ring homomorphism.
>
> {:.par-upper-roman}
> 1. $$ f(0)=0. $$
>
> 2. For all $a\in R$,
>
>    $$ f(-a) = -f(a). $$

**Proof.**{:.run-in}By [GH#PROP-A](/codex/GH#PROP-A).<qed />

> {{ "RH#PROP-MON" | cod }}
>
> Let $f:R\to S$ be a ring homomorphism. $f$ is a monomorphism if and only if
>
> $$ \ker f = \{0\}. $$

**Proof.**{:.run-in}By [GH#PROP-MON](/codex/GH#PROP-MON).<qed />

