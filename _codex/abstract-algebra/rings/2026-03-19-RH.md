---
layout: carbon
title: Ring Homomorphism
updated: 2026-03-19
---

> {{ "DEF-RH:Ring Homomorphism" | codexify }}
>
> Let $R$ and $S$ be two rings. A **ring homomorphism** between $R$ and $S$ is a function $f:R\to S$ such that:
>
> {:.par-upper-roman}
> 1. $$ f(a+b) = f(a)+f(b) $$
>
>    for all $a$, $b\in R$.
>
> 2. $$ f(a\cdot b) = f(a)\cdot f(b) $$
>
>    for all $a$, $b\in R$.
>
> 3. $$ f(1) = 1. $$
>
> Namely, $f$ is both a group homomorphism from $(R,+)$ to $(S,+)$ and a monoid homomorphism from $(R,\cdot)$ to $(S,\cdot)$.

> {{ "DEF-RH-TRI:Trivial Ring Homomorphism" | codexify }}
>
> The **trivial / zero ring homomorphism** between two rings $R$ and $S$ is the function
>
> $$ f : R\to S, \, a\mapsto 0. $$

> {{ "DEF-RH-KER:Kernel" | codexify }}
>
> The **kernel** of a ring homomorphism $f:R\to S$ is
>
> $$ \ker f = f^{-1}[\{0\}]. $$

---

> {{ "PROP-RH-A" | codexify }}
>
> Let $f:R\to S$ is a ring homomorphism.
>
> {:.par-upper-roman}
> 1. $$ f(0)=0. $$
> 2. $$ f(-a) = -f(a) $$
>
>    for every $a\in R$.

**Proof.**{:.run-in}By [PROP-GH-A](/codex/GH#PROP-GH-A).<qed />

> {{ "PROP-RH-MON" | codexify }}
>
> Let $f:R\to S$ be a ring homomorphism. $f$ is a monomorphism if and only if
>
> $$ \ker f = \{0\}. $$

**Proof.**{:.run-in}By [PROP-GH-MON](/codex/GH#PROP-GH-MON).<qed />

