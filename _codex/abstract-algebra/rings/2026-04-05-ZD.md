---
layout: carbon
title: Zero Divisor
updated: 2026-04-05
---

> {{ "DEF-ZD:Zero Divisor" | codexify }}
>
> Let $R$ be a ring and $a\in R$.
>
> {:.no-indent}
> - $a$ is a **left zero divisor** if there exists $x\in R^*$ such that
>
>   $$ ax = 0. $$
>
> - $a$ is a **right zero divisor** if there exists $x\in R^*$ such that
>
>   $$ xa = 0. $$
>
> - $a$ is a **zero divisor** if it is a left zero divisor or right zero divisor.

